<h1><%= locName %></h1>
<h4><%= new Date(weatherInfo.currently.time) %></h4>
<form action="/locations" method="POST">
  <input type="hidden" name="city" value="<%= locName %>">
  <input type="hidden" name="lat" value="<%= weatherInfo.latitude %>">
  <input type="hidden" name="long" value="<%= weatherInfo.longitude %>">

  <input type="submit" value="Add to Your Locations">
</form>

<canvas id="icon" width="64px" height="64px"></canvas>
<script src="https://cdnjs.cloudflare.com/ajax/libs/skycons/1396634940/skycons.js"></script>
<script>
  switch (exp) {
    
  }
</script>
<p><%= weatherInfo.currently.summary %></p>

<div class="info-container">
  <div class="left">
    <h1><%= weatherInfo.currently.temperature %>°F</h1>
    <h3><%= weatherInfo.currently.summary %></h3>

  </div>

  <div class="right">
    <h6>Wind: <%= weatherInfo.currently.windSpeed %>mph</h6>
    <h6>Precipitation: <%= weatherInfo.currently.precipProbability * 100 %>%</h6>
    <h6>Humidity: <%= weatherInfo.currently.humidity * 100 %>%</h6>
  </div>
</div>
<div class="daily-info">

</div>

<div class="trail-container">
  <div class="trail">
  <% trails.forEach(function(trail) { %>
    <h4 class="inline"><%= trail.name %></h4>
    <form action="/trails/<%= trail.id %>">
      <input type="submit" value="View Details">
    </form>
    <form action="/trails" method="POST">
      <input type="hidden" name="name" value="<%= trail.name %>">
      <input type="hidden" name="idnum" value="<%= trail.id %>">
      <input type="submit" value="Add to Your Trails">
    </form>
  </div>
  <% }); %>
</div>
