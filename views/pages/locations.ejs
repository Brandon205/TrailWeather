<h1>List of users locations here</h1>

<% locations.forEach(function(location) { %>
  <div>
    <h3><%= location.city %></h3>
    <h4><%= location.lat %>, <%= location.long %></h4>
    <form action="/locations/<%= location.lat %>/?_method=DELETE" method="POST">
      <input type="submit" value="Delete">
    </form>
  </div>
<% }) %>