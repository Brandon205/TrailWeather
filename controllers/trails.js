const express = require('express');
const router = express.Router();
const db = require('../models');

// GET to /trails > show all of the users saved trails

// GET to /trails:id > shows more details on a specific trail

// POST to /trails > add a new trail to the users saved list

module.exports = router;
